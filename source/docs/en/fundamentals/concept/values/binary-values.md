---
extends: _core._layouts.documentation
section: content
title: "Binary values"
description: "Binary values"
---

# Binary values

Binary values are used for properties that can be either enabled or disabled:

1. For example, `italic` (enabled) and `italic-none` (disabled).
2. Other examples: `visible`, `visible-none`.
