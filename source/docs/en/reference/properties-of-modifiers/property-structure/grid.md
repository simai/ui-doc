---
extends: _core._layouts.documentation
section: content
title: Grid (grid)
description: Grid (grid)
---

# Grid (grid)

* Grid column template (grid-template-columns).
* Grid column size (grid-column).
* Grid column start position (grid-column-start).
* Grid column end position (grid-column-end).
* Automatic grid column size (grid-auto-columns).
* Grid row template (grid-template-rows).
* Grid row size (grid-row).
* Grid row start position (grid-row-start).
* Grid row end position (grid-row-end).
* Automatic grid row size (grid-auto-rows).
* Automatic grid filling (grid-auto-flow).
