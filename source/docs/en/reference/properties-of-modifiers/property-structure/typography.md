---
extends: _core._layouts.documentation
section: content
title: Typography (typography)
description: Typography (typography)
---

# Typography (typography)

* Default parameters (header).
* Headings (header).
* Font size (font-size).
* Font family (font-family).
* Font smoothing (font-smoothing).
* Font style (font-style).
* Font weight (font-weight).
* Small caps (font-variant).
* Numeric font variant (font-variant-numeric).
* Text transform (text-transform).
* Line height (line-height).
* Letter spacing (letter-spacing).
* Text indent (text-indent).
* Line length (text max-width).
* Horizontal alignment (text-align).
* Vertical alignment (vertical-align).
* List marker style (list-style-type).
* List marker position (list-style-position).
* Text overflow (text-overflow).
* Whitespace handling (white-space).
* Word breaking (word-break).
* Text highlight (mark).
* Content control (content).
