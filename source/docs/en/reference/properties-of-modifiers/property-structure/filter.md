---
extends: _core._layouts.documentation
section: content
title: Element filters (filter)
description: Element filters (filter)
---

# Element filters (filter)

* Element blur (filter-blur).
* Element opacity (filter-opacity).
* Element brightness (filter-brightness).
* Element contrast (filter-contrast).
* Element grayscale (filter-grayscale).
* Element hue rotation (filter-hue-rotate).
* Element invert (filter-invert).
* Element saturation (filter-saturate).
* Element sepia (filter-sepia).
