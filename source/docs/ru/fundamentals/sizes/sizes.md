---
extends: _core._layouts.documentation
section: content
title: Система размеров
description: "Базовая шкала размеров, семантические интервалы и правила использования размеров SIMAI Framework."
---

# Система размеров

В основе размеров SIMAI Framework лежат 90 примитивов от `--sf-a0` до
`--sf-i9`. Они покрывают диапазон от `0` до `2432px` и используются как
строительные блоки для интервалов, типографики, радиусов, контейнеров и утилит.

- [Полная шкала размеров](/ru/fundamentals/sizes/size-scale/) — каждый токен в `rem` и `px`.
- [Конвертация размеров](/ru/fundamentals/sizes/size-conversion/) — формула буквенно-цифровой записи.
- [Интервалы](/ru/fundamentals/sizes/indents/) — семантические отступы.
- [Мнемоника](/ru/fundamentals/sizes/mnemonic/) — как быстрее запомнить шкалу.

## Принцип шкалы

Каждая буква содержит десять значений. При переходе к следующей группе
увеличивается шаг:

| Группа | Диапазон | Шаг |
|:---|---:|---:|
| `a0`–`a9` | 0–9px | 1px |
| `b0`–`b9` | 10–19px | 1px |
| `c0`–`c9` | 20–38px | 2px |
| `d0`–`d9` | 40–76px | 4px |
| `e0`–`e9` | 80–152px | 8px |
| `f0`–`f9` | 160–304px | 16px |
| `g0`–`g9` | 320–608px | 32px |
| `h0`–`h9` | 640–1216px | 64px |
| `i0`–`i9` | 1280–2432px | 128px |
{.table}

Значения хранятся в `rem` при базовом размере шрифта `16px`. Например,
`--sf-c2` равно `1.5rem`, то есть `24px`.

## Что выбирать в проекте

Для обычных отступов и размеров элементов сначала используйте семантические
токены (`--sf-space-*`, размеры текста, иконок и UI). К примитивам `--sf-a0` —
`--sf-i9` обращайтесь, когда нужен точный размер или проектный токен.

Высоту и вертикальные интервалы рекомендуется собирать с ритмом, кратным `4px`,
если это не нарушает типографику или требования конкретного компонента.
