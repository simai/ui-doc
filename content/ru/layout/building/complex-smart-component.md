---
title: "Сложный Smart-компонент"
description: "Сложный Smart-компонент объединяет несколько интерактивных частей и открывает именованные области для вложенного содержимого."
---

# Сложный Smart-компонент

Сложный Smart-компонент объединяет несколько интерактивных частей и открывает именованные области для вложенного содержимого. Его контракт особенно важен: он определяет, что можно вставить в каждую область и какие ресурсы нужны целому составу.

## Когда применять

Используйте такой тип для устойчивого повторяемого сценария, например таблицы с фильтрами или мастера шагов. Не превращайте в него уникальную страницу, если её части не образуют повторяемый компонент.

## Пример

Будущий тип `smart.product-browser` может иметь области `filters`, `results` и `pagination`. Manifest должен подтвердить эти области в реальной реализации. До этого макет каталога следует собирать из базовых типов или проектного блока. Framework намеренно выдаёт ошибку для неизвестного слота вместо догадки.

## Что дальше

[Разберитесь с именованными областями](/ru/layout/building/slots/).
