---
title: "Цвет тени элемента"
description: "Цвет тени для default / hover / active"
tags: [box-shadow-color, hover, active]
---

# Цвет тени элемента

:badge[box-shadow-color]{type=main scheme=on-surface size=1}

Утилиты задают цвет тени в состояниях `default`, `hover`, `active`.

## Наглядный пример

:::example {id="utilities/shadows/element-color" label="Цвет тени элемента"}
:::

## Таблица классов

| Класс | Значение |
|:--|:--|
| `shadow-{color}` | цвет тени в базовом состоянии |
| `hover:shadow-{color}` | цвет тени в `:hover` |
| `active:shadow-{color}` | цвет тени в `:active` |

Поддерживаемые `color`: `on-surface`, `primary`, `secondary`, `tertiary`, `error`, `success`, `warning`.

## Синтаксис

- `shadow-{color}`
- `hover:shadow-{color}`
- `active:shadow-{color}`

## Пример
