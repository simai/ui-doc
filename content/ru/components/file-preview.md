---
title: "Предпросмотр файла"
description: "Компактное представление выбранного файла с именем, размером и доступными действиями."
profile: reference
---

# Предпросмотр файла

Компонент показывает выбранный или прикреплённый файл: его тип, имя, размер и
доступные действия. Он подходит для списков вложений и состояния файла перед
отправкой формы.

## Пример

:::example {id="components/file-preview/overview" label="Результат"}
:::

## Особенности применения

Имя файла должно оставаться доступным текстом. Действия оформляйте нативными
кнопками с доступными именами, а декоративные иконки скрывайте от экранного
диктора с помощью `aria-hidden="true"`. Компонент не описывает ход загрузки:
для прогресса и повторной отправки используйте File Upload.

## Миниатюра

Вместо иконки типа файла в держателе можно показать изображение с классом
`sf-file-preview-thumbnail`.

:::example {id="components/file-preview/thumbnail" label="Результат"}
:::

## Поверхность

Модификатор `sf-file-preview--surface` добавляет собственную крупную поверхность
и системные внутренние отступы.

:::example {id="components/file-preview/surface" label="Результат"}
:::

## Недоступное состояние

Класс `disabled` обозначает файл, с которым временно нельзя взаимодействовать.
Недоступность действий дополнительно задавайте нативным атрибутом `disabled`.

:::example {id="components/file-preview/disabled" label="Результат"}
:::
