---
title: "Закрытие"
description: "Стандартная кнопка закрытия панели, окна или уведомления."
profile: reference
---

# Закрытие

Стандартная кнопка закрытия панели, окна или уведомления. Пример ниже исполняется тем же кодом, который доступен
во вкладках исходников.

## Быстрый старт

:::example {id="components/close/overview" label="Результат"}
:::

## Когда использовать

- Используйте компонент, когда нужно: Стандартная кнопка закрытия панели, окна или уведомления.
- Выберите вариант и размер по иерархии задачи, плотности интерфейса и доступному месту.
- Не используйте компонент как декоративный элемент: действие должно иметь понятный результат.

## Варианты и состояния

Проверенные варианты, размеры и состояния собраны в примере. Переключитесь на
вкладку HTML, чтобы скопировать точную публичную разметку. Состояния задавайте
нативными атрибутами (`disabled`, `checked`, `aria-*`) и публичными классами
компонента; не повторяйте внутренние стили в CSS проекта.

## Доступность

Используйте нативный `<button>` для действий и `<a>` для переходов. У элемента должно быть доступное имя; состояние `disabled` нельзя обозначать только цветом.

Проверьте видимый фокус, контраст, чтение экранным диктором и работу при
увеличении текста до 200% в контексте своего интерфейса.

## Технический контракт

- Loader-идентификатор: `close`.
- Поставка: SIMAI Framework 5.4.0.
- Источник истины: Framework Registry и ассеты `distr/component/close`.
- Подключение выполняет Loader после обнаружения публичных классов компонента;
  отдельный проектный CSS для примера не требуется.

[Вернуться к каталогу компонентов](/ru/components/).
